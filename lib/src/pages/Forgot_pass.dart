import 'package:flutter/material.dart';

class Forgot_pas extends StatelessWidget {
  const Forgot_pas({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
